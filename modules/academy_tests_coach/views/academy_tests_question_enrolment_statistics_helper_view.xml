<?xml version="1.0" encoding="UTF-8"?>

<openerp>
    <data noupdate="0">

        <!-- VIEWS: "Question statistics for enrolment"

            - Model·········: academy.tests.question.enrolment.statistics.helper
            - _rec_name·····: id
            - Domain········: []
            - Context·······: {}
            - Parent menu ··: base.menu_custom
            - View priority·: 16    Sequence····: 10
            - Action help···: There is not statistics
        - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -->

        <!-- Tree view for "academy.tests.question.enrolment.statistics.helper" -->
        <record id="view_academy_tests_question_enrolment_statistics_helper_tree" model="ir.ui.view">
            <field name="name">view.academy.tests.question.enrolment.statistics.helper.tree</field>
            <field name="model">academy.tests.question.enrolment.statistics.helper</field>
            <field name="type">tree</field>
            <field name="mode">primary</field>
            <field name="priority" eval="16" />
            <field name="active" eval="True" />
            <field name="inherit_id" ref="academy_tests.view_academy_tests_abstract_question_statistics_tree" />
            <field name="arch" type="xml">
                <xpath expr="//field[@name='id']" position="replace">
                    <field name="enrolment_id" class="oe_field_enrolment_id" />
                </xpath>
            </field>
        </record>

        <!-- Form view for "academy.tests.question.enrolment.statistics.helper" -->
        <record id="view_academy_tests_question_enrolment_statistics_helper_form" model="ir.ui.view">
            <field name="name">view.academy.tests.question.enrolment.statistics.helper.form</field>
            <field name="model">academy.tests.question.enrolment.statistics.helper</field>
            <field name="type">form</field>
            <field name="mode">primary</field>
            <field name="priority" eval="16" />
            <field name="active" eval="True" />
            <field name="inherit_id" ref="academy_tests.view_academy_tests_abstract_question_statistics_form" />
            <field name="arch" type="xml">
                <xpath expr="//field[@name='id']" position="replace">
                    <field name="enrolment_id" class="oe_field_enrolment_id" />
                </xpath>
            </field>
        </record>

        <!-- Search view for "academy.tests.question.enrolment.statistics.helper" -->
        <record id="view_academy_tests_question_enrolment_statistics_helper_search" model="ir.ui.view">
            <field name="name">view.academy.tests.question.enrolment.statistics.helper.search</field>
            <field name="model">academy.tests.question.enrolment.statistics.helper</field>
            <field name="type">search</field>
            <field name="mode">primary</field>
            <field name="priority" eval="16" />
            <field name="active" eval="True" />
            <field name="inherit_id" ref="academy_tests.view_academy_tests_abstract_question_statistics_search" />
            <field name="arch" type="xml">
                <xpath expr="//field[@name='id']" position="replace">
                    <field name="enrolment_id" class="oe_field_enrolment_id" />
                </xpath>
            </field>
        </record>

        <!-- Main window action for model.name -->
        <record id="action_question_statistics_for_enrolment_act_window" model="ir.actions.act_window">
            <field name="type">ir.actions.act_window</field>
            <field name="name">Question statistics for enrolment</field>
            <field name="res_model">academy.tests.question.enrolment.statistics.helper</field>
            <field name="view_mode">tree,form</field>
            <field name="target">current</field>
            <field name="domain">[]</field>
            <field name="context">{}</field>
            <field name="search_view_id" ref="view_academy_tests_question_enrolment_statistics_helper_search" />
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    Click to add new question statistics for enrolment
                </p><p>
                    There is not statistics
                </p>
            </field>
        </record>

        <!-- Main menu for model.name -->
        <record id="menu_question_statistics_for_enrolment" model="ir.ui.menu" >
            <field name="name">Statistics by enrolment</field>
            <field name="sequence" eval="10" />
            <field name="action" ref="action_question_statistics_for_enrolment_act_window" />
            <field name="parent_id" ref="menu_test_settings_top" />
        </record>

    </data>
</openerp>