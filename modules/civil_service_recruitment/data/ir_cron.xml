<?xml version="1.0" encoding="utf-8"?>

<odoo noupdate="0">

    <record id="ir_cron_civil_service_recruitment_update_states_daily" model="ir.cron">
        <field name="name">Civil service recruitment: Update states</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="nextcall" eval="(DateTime.now().replace(hour=2, minute=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')" />
        <field name="doall" eval="False"/>
        <field name="model_id" ref="model_civil_service_recruitment_process"/>
        <field name="code">model.update_states()</field>
        <field name="state">code</field>
    </record>

    <record id="action_update_states_server" model="ir.actions.server">
        <field name="type">ir.actions.server</field>
        <field name="name">Update states</field>
        <field name="model_id" ref="model_civil_service_recruitment_process" />
        <field name="sequence" eval="5" />
        <field name="binding_model_id" ref="civil_service_recruitment.model_civil_service_recruitment_process" />
        <field name="state">code</field>
        <field name="code">action = model.update_states()</field>
    </record>
    
</odoo>

